services:
  horizon:
    build: .
    container_name: horizon
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    environment:
      - TZ=America/Los_Angeles
    restart: "no"
    # For one-time run, use: docker compose run --rm horizon
    command: ["--hours", "36"]
